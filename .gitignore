# Runtime secrets — never commit
.secret_key
config.json
profiles.json
players.json
geoip_cache.json
chat_log.json
wipe_history.log

# Python cache
__pycache__/
*.pyc
*.pyo

# Release archives (current install.sh is the source of truth)
releases/
